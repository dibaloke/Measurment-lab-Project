
import 'package:flutter/material.dart';

class Consumption extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        backgroundColor: Color.fromRGBO(0, 160, 247 ,0.5),
        title: Text("Consumption"),
        
      ),
      body:Container()
    );
  }
}

